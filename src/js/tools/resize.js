
// goya.tools.resize

{
	init() {
		
	},
	dispatch(event) {
		let APP = goya,
			Self = APP.tools.resize,
			el;
		// console.log(event);
		switch (event.type) {
			case "blur-tool":
				break;
			case "focus-tool":
				break;
		}
	}
}