
// goya.sidebar.brush

{
	init() {

	},
	dispatch(event) {
		let APP = goya,
			Self = APP.sidebar.brush,
			name,
			value,
			pEl,
			el;
		// console.log(event);
		switch (event.type) {
			case "set-variable":
				break;
		}
	}
}
