
let Test = {
	init(APP) {
		// setTimeout(() => window.find(`.toolbar-tool_[data-arg="resize"]`).trigger("click"), 500);

		// setTimeout(() => window.find(`.sidebar-head span:nth(1)`).trigger("click"), 500);

	}
};
